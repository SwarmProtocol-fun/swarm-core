"use client";
/** Stub — Hero 3D scene extracted to mod. Placeholder fallback. */
export function RobotSwarm3D() {
  return (
    <div className="w-full h-[400px] flex items-center justify-center bg-gradient-to-b from-background to-muted rounded-xl">
      <p className="text-muted-foreground text-sm">3D Scene — Install hero mod to enable</p>
    </div>
  );
}
