"use client";
/** Stub — ReactBits ClickSpark extracted to mod. No-op fallback. */
export default function ClickSpark({ children }: { children: React.ReactNode; sparkColor?: string; sparkSize?: number; sparkRadius?: number; sparkCount?: number; duration?: number; [k: string]: unknown }) {
  return <>{children}</>;
}
