"use client";
/** Stub — ReactBits DecryptedText extracted to mod. Plain text fallback. */
export default function DecryptedText({ text, className }: { text: string; className?: string; animateOn?: string; speed?: number; sequential?: boolean; [k: string]: unknown }) {
  return <span className={className}>{text}</span>;
}
